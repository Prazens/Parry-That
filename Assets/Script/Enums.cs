using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum Direction
{
    None = 0,
    Up = 1,
    Down = 2,
    Left = 3,
    Right = 4
}

// 너무 헷갈려서 만듬
public enum AttackType
{
    Normal = 0,
    Strong = 1,
    HoldStart = 2,
    HoldFinish = 3
}